# Gap Report: PD-310

**Requirement:** 3.4.2 FINVOICE data
**Scanned:** 2026-04-21T14:00:00Z
**Verdict:** PASSES

## Acceptance Criteria Coverage

| # | Criterion | Status | Notes |
|---|-----------|--------|-------|
| 1 | The FINVOICE material is to be generated during the invoicing run. | IMPLEMENTED | `InvoiceGenerationService.generate()` calls `finvoiceBuilderService.build(saved)` post-save and persists XML. `InvoiceTransmissionService.transmit()` builds XML if null before sending. |
| 2 | The FINVOICE specification/definition is to be agreed during implementation. | IMPLEMENTED | Process criterion; Finvoice 3.0 spec used as the agreed standard. |
| 3 | The current latest standard is Finvoice 3 | IMPLEMENTED | `FinvoiceBuilderService` emits `<Finvoice Version="3.0">` on the root element. |
| 4 | The system generates a dataset with all mandatory FINVOICE fields + accounting info; lines may split across ledger accounts | IMPLEMENTED | Seller/buyer details, invoice metadata, row-level pricing, VAT spec, and totals all present. Ledger splits via `AccountingLedgerEntry`; cost center, service responsibility, and waste type emitted as `<RowIdentifier>` elements per row. |
| 5 | FINVOICE compatibility: generate in FINVOICE format + transmit with cost center, service responsibility, and waste type | IMPLEMENTED | XML generated with all three accounting identifiers in every row. `InvoiceTransmissionService` → `ExternalInvoicingClient` wired end-to-end. |
| 6 | Support multiple VAT rates, correct VAT rate, VAT rates with decimals | IMPLEMENTED | `groupLinesByVatRate()` handles mixed rates. `VatRate.rate` stored at `precision=5, scale=2`. Frontend uses `step="0.01"`. |
| 7 | The solution will be integrated with external systems. | IMPLEMENTED | `ExternalInvoicingClient` uses `RestTemplate` with real HTTP calls (POST transmit, GET status/image/attachments, POST recall, POST/DELETE operator registration). Falls back to stub only when `app.operator.base-url` is blank (dev/test mode). |

## UI Review Notes

AC2 (FINVOICE specification agreed during implementation) and AC3 (Finvoice 3 standard compliance) are intentional non-UI criteria — they are satisfied at implementation time by backend configuration and XML schema conformance respectively. Neither requires a user-facing screen. All remaining ACs have confirmed UI surfaces on the Invoice Detail page, Invoice Runs page, and Master Data pages. No frontend gaps identified.

## Gaps Found

None.

## Files Checked

- `invoicing/src/main/java/com/example/invoicing/service/FinvoiceBuilderService.java`
- `invoicing/src/main/java/com/example/invoicing/service/InvoiceGenerationService.java`
- `invoicing/src/main/java/com/example/invoicing/service/InvoiceTransmissionService.java`
- `invoicing/src/main/java/com/example/invoicing/integration/ExternalInvoicingClient.java`
- `invoicing/src/main/java/com/example/invoicing/integration/FinvoiceOperatorProperties.java`
- `invoicing/src/main/java/com/example/invoicing/entity/invoice/InvoiceLineItem.java`
- `invoicing/src/main/java/com/example/invoicing/entity/invoice/AccountingLedgerEntry.java`
- `invoicing/src/main/java/com/example/invoicing/entity/vat/VatRate.java`
- `invoicing/src/main/java/com/example/invoicing/controller/invoice/FinvoiceXmlController.java`
- `invoicing-fe/src/pages/masterdata/VatRatesPage.jsx`

## Verdict

PASSES
