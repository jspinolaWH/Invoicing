# Gap Report: PD-294

**Requirement:** 3.4.18 Billing surcharge
**Scanned:** 2026-04-21T18:00:00Z
**Verdict:** PASSES

## Acceptance Criteria Coverage

| # | Criterion | Status | Notes |
|---|-----------|--------|-------|
| 1 | Surcharge added per invoicing method; auto-generated; global toggle on/off | IMPLEMENTED | `InvoiceGenerationService` calls `BillingSurchargeService.resolveConfig(deliveryMethod, customerType)`; global toggle via `globalSurchargeEnabled` flag |
| 2 | Surcharges can differ by customer type (business vs consumer); linkable to paper/direct-pay/email | IMPLEMENTED | `SurchargeConfig.customerType` field + repository queries for type-specific and fallback (null = all types) configs |
| 3 | Separate product per surcharge; shown automatically; user can switch off; amount definable per customer type | IMPLEMENTED | Surcharge appended as line item during generation; `InvoiceDetailPage` has Remove Surcharge button; customerType on `SurchargeConfig` enables per-type amounts |
| 4 | Situation-specific exceptions can be applied (header AC) | IMPLEMENTED | `exemptFirstInvoice` and `requiresTariffInclusion` flags on `SurchargeConfig`; both checked in `InvoiceGenerationService` |
| 5 | Removing the surcharge from the first invoice | IMPLEMENTED | `InvoiceGenerationService` checks `isFirstInvoice` (no prior invoices for customer) and skips surcharge if `exemptFirstInvoice = true` |
| 6 | Preventing surcharges when not included in the tariff | IMPLEMENTED | `InvoiceGenerationService` checks `requiresTariffInclusion` against active contracts; skips surcharge if no active contract |
| 7 | Allowing different surcharges for business and consumer customers | IMPLEMENTED | `SurchargeConfig.customerType` (nullable) with fallback logic in `BillingSurchargeService.resolveConfig()` |
| 8 | Manually removing individual surcharges from individual invoices | IMPLEMENTED | `POST /api/v1/invoices/{id}/remove-surcharge` + "Remove Surcharge" button in `InvoiceDetailPage.jsx` |
| 9 | Defining different surcharge amounts for business and consumer customers | IMPLEMENTED | Per-customerType `SurchargeConfig` records with distinct `amount` values; service picks most-specific match |
| 10 | Waste companies can define cases where system does not apply surcharges | IMPLEMENTED | Global toggle, per-config `active` flag, `exemptFirstInvoice`, `requiresTariffInclusion` all configurable via `SurchargeConfigPage` |
| 11 | Surcharges can vary based on customer types | IMPLEMENTED | Customer type (PRIVATE/BUSINESS/MUNICIPALITY/AUTHORITY) passed to surcharge resolution; null config = applies to all |
| 12 | Users can manually remove surcharges from individual invoices | IMPLEMENTED | Backend endpoint + frontend button in `InvoiceDetailPage.jsx`; button only shown when surcharge line items exist |
| 13 | Surcharge amount is visible on the invoice sent to the customer | IMPLEMENTED | Surcharge line item with description "Invoicing surcharge" rendered by `InvoiceLineItemsTable` |

## Files Checked

- `invoicing/src/main/java/com/example/invoicing/service/InvoiceGenerationService.java`
- `invoicing/src/main/java/com/example/invoicing/entity/surcharge/SurchargeConfig.java`
- `invoicing/src/main/java/com/example/invoicing/service/BillingSurchargeService.java`
- `invoicing/src/main/java/com/example/invoicing/repository/SurchargeConfigRepository.java`
- `invoicing/src/main/java/com/example/invoicing/entity/surcharge/dto/SurchargeConfigRequest.java`
- `invoicing/src/main/java/com/example/invoicing/entity/surcharge/dto/SurchargeConfigResponse.java`
- `invoicing-fe/src/pages/masterdata/SurchargeConfigPage.jsx`
- `invoicing-fe/src/pages/invoices/InvoiceDetailPage.jsx`

## Verdict

PASSES
