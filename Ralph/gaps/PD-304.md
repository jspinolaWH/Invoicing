# Gap Report: PD-304

**Requirement:** 3.4.8 PDF attachment to invoice batch
**Scanned:** 2026-04-21T07:00:00Z
**Verdict:** GAPS_FOUND

## Acceptance Criteria Coverage

| # | Criterion | Status | Notes |
|---|-----------|--------|-------|
| 1 | Single PDF attached to entire invoice batch via FINVOICE AttachmentIdentifier | IMPLEMENTED | `propagateBatchAttachment()` in orchestrator creates InvoiceAttachment for every generated invoice; `appendAttachmentDetails()` includes it in FINVOICE XML |
| 2 | Users define PDF attachment when creating batch; AttachmentDetails + AttachmentIdentifier in FINVOICE | IMPLEMENTED | `InvoiceRunPage.jsx` has UI fields (identifier, filename, mimeType, securityClass); `InvoiceRunService` persists them to `InvoiceRun`; FINVOICE builder includes them |
| 3 | Attachment references correctly formatted and validated in FINVOICE | IMPLEMENTED | `FinvoiceBuilderService.appendAttachmentDetails()` writes AttachmentIdentifier, AttachmentName, AttachmentMimeType, AttachmentSecurityClass, AttachmentContent |
| 4 | Invoicing service associates attachment with invoice batch via identifier | IMPLEMENTED | `ExternalInvoicingClient.verifyAttachment()` validates identifier against external service before send; `propagateBatchAttachment()` links to every invoice |
| 5 | Batch-level attachments: single document attached to whole batch | IMPLEMENTED | `InvoiceRunOrchestratorService.propagateBatchAttachment()` called per invoice during run execution |
| 6 | Attachment metadata in FINVOICE (AttachmentDetails section) | IMPLEMENTED | `FinvoiceBuilderService` line 438–452: AttachmentDetails element with all required child elements |
| 7 | Automatic validation of attachment metadata before submission | IMPLEMENTED | `InvoiceRunService` line 90 calls `verifyAttachment()` before triggering send; `InvoiceAttachmentService` validates MIME type, size, count |
| 8 | Audit and compliance: log all invoice-attachment associations | IMPLEMENTED | `InvoiceRunOrchestratorService` line 180: `log.info("AUDIT batch_attachment_linked runId={} invoiceId={} attachmentIdentifier={} at={}")` |
| 9 | Error handling if attachment identifier doesn't match invoicing service file | IMPLEMENTED | `verifyAttachment()` returns false → throws `InvalidRunStateException` with descriptive message before submission |

## Files Checked

- `invoicing/src/main/java/com/example/invoicing/entity/invoicerun/InvoiceRun.java` — batch attachment fields (lines 103–113)
- `invoicing/src/main/java/com/example/invoicing/entity/invoice/dto/BatchAttachmentRequest.java`
- `invoicing/src/main/java/com/example/invoicing/entity/invoicerun/dto/InvoiceRunResponse.java`
- `invoicing/src/main/java/com/example/invoicing/controller/run/InvoiceRunController.java` — `POST /{id}/batch-attachment` endpoint (line 53)
- `invoicing/src/main/java/com/example/invoicing/service/InvoiceRunService.java` — verifyAttachment call (line 90), batch attachment persistence
- `invoicing/src/main/java/com/example/invoicing/service/InvoiceRunOrchestratorService.java` — `propagateBatchAttachment()` (line 160), audit log (line 180)
- `invoicing/src/main/java/com/example/invoicing/service/FinvoiceBuilderService.java` — `appendAttachmentDetails()` (line 438), called at line 75
- `invoicing/src/main/java/com/example/invoicing/service/InvoiceAttachmentService.java` — upload validation, PDF/A compliance check
- `invoicing/src/main/java/com/example/invoicing/integration/ExternalInvoicingClient.java` — `verifyAttachment()` (line 188)
- `invoicing-fe/src/pages/runs/InvoiceRunPage.jsx` — batch attachment UI fields (lines 13–108)
- `invoicing-fe/src/api/invoiceRuns.js` — `setBatchAttachment()` API call (line 9)
- `invoicing-fe/src/api/invoiceAttachments.js` — `setBatchAttachment()` (line 21)
- `invoicing-fe/src/pages/invoices/components/AttachmentUploadZone.jsx`

## Gaps

### Gap 1 — AC3: No human-readable summary of AttachmentDetails on the Invoice Detail page

**Layer:** Frontend

**AC:** AC3 — Attachment references correctly formatted and included in FINVOICE message

**What is missing from the UI:** The UI provides a raw FINVOICE XML download but does not display a human-readable summary of the `AttachmentDetails` contents (e.g., the identifier, filename, and MIME type) on the invoice detail page. A tester cannot confirm correct formatting without opening the XML file externally.

**UI surface description:** Invoice Detail page → Attachments panel (`InvoiceAttachmentsPanel.jsx` / `AttachmentsList.jsx`). The attachment list rows show filename, size, and security class, but do not surface the `AttachmentIdentifier`, `AttachmentName`, and `AttachmentMimeType` values that were embedded from the batch run as a parsed on-screen summary.

**Files to change:**
- `/mnt/c/Users/drasm/Desktop/Invoicing/invoicing-fe/src/pages/invoices/components/InvoiceAttachmentsPanel.jsx`
- `/mnt/c/Users/drasm/Desktop/Invoicing/invoicing-fe/src/pages/invoices/components/AttachmentsList.jsx`

---

### Gap 2 — AC5: No on-screen display of AttachmentDetails from the batch run

**Layer:** Frontend

**AC:** AC5 — Attachment reference included in FINVOICE message with AttachmentDetails

**What is missing from the UI:** There is no on-screen display of `AttachmentDetails` metadata (identifier, filename, MIME type) on the Invoice Detail page or the Invoice Run Detail page. Users must download and inspect the raw FINVOICE XML to verify the attachment reference is present and correct.

**UI surface description:** Invoice Detail page → Attachments section and Invoice Run Detail page (`InvoiceRunDetailPage.jsx` / `RunSummaryCard.jsx`). Neither surface shows a parsed `AttachmentDetails` block; the only confirmation path is manual XML inspection via the "↓ FINVOICE XML" button.

**Files to change:**
- `/mnt/c/Users/drasm/Desktop/Invoicing/invoicing-fe/src/pages/invoices/components/InvoiceAttachmentsPanel.jsx`
- `/mnt/c/Users/drasm/Desktop/Invoicing/invoicing-fe/src/pages/runs/InvoiceRunDetailPage.jsx`
- `/mnt/c/Users/drasm/Desktop/Invoicing/invoicing-fe/src/pages/runs/components/RunSummaryCard.jsx`

---

### Gap 3 — AC9: No parsed AttachmentDetails panel on Invoice Detail or Run Detail

**Layer:** Frontend

**AC:** AC9 — AttachmentDetails in FINVOICE message with correct linking

**What is missing from the UI:** No dedicated on-screen summary of the FINVOICE `AttachmentDetails` section is shown on the Invoice Detail or Run Detail pages. The attachment identifier is visible in the attachment list as a small code label, but the full `AttachmentDetails` block (identifier, filename, MIME type, security class) is not presented as a structured panel that a user can quickly review.

**UI surface description:** Invoice Detail page → Attachments list rows (`AttachmentsList.jsx`) show a small `attachmentIdentifier` code label but no structured `AttachmentDetails` summary. Invoice Run Detail page has no attachment detail section at all.

**Files to change:**
- `/mnt/c/Users/drasm/Desktop/Invoicing/invoicing-fe/src/pages/invoices/components/AttachmentsList.jsx`
- `/mnt/c/Users/drasm/Desktop/Invoicing/invoicing-fe/src/pages/runs/InvoiceRunDetailPage.jsx`

---

### Gap 4 — AC10: No client-side validation of attachment identifier or MIME type before run submission

**Layer:** Frontend

**AC:** AC10 — Automatic validation of attachment metadata before submission

**What is missing from the UI:** No client-side pre-validation of the attachment identifier format or MIME type value is performed before clicking "Start Real Run". The MIME Type field has no dropdown or enforced format, and the identifier field has no format check. All validation happens server-side after the full run submission, meaning users receive errors only after a complete round-trip rather than immediately on form input.

**UI surface description:** Invoice Runs page → Batch PDF Attachment (optional) panel (`InvoiceRunPage.jsx`). The "Attachment Identifier" input and "MIME Type" input fields have no format validation attributes (`pattern`, dropdown constraint, or inline format hint). Error feedback is only surfaced after "Start Real Run" is clicked.

**Files to change:**
- `/mnt/c/Users/drasm/Desktop/Invoicing/invoicing-fe/src/pages/runs/InvoiceRunPage.jsx`

---

### Gap 5 — AC11: No UI surface for the invoice–attachment association audit log

**Layer:** Frontend

**AC:** AC11 — Audit and compliance: logs all invoice–attachment associations

**What is missing from the UI:** There is no UI surface for viewing an audit log of invoice–attachment associations. The Invoice Detail page shows a general "Reporting Data Audit Trail" table for reporting field changes, but this does not include attachment association events. The backend logs these associations (via `log.info("AUDIT batch_attachment_linked ...")` in `InvoiceRunOrchestratorService`) but the log data is never surfaced in any frontend view.

**UI surface description:** Invoice Detail page → Reporting Data Audit Trail section (bottom of page). The existing audit table tracks line-item reporting field changes only. No panel or tab exists to show when attachments were linked to an invoice during a batch run, which attachment identifier was used, and which run triggered the linkage.

**Files to change:**
- `/mnt/c/Users/drasm/Desktop/Invoicing/invoicing-fe/src/pages/invoices/InvoiceDetailPage.jsx`
- `/mnt/c/Users/drasm/Desktop/Invoicing/invoicing-fe/src/pages/runs/InvoiceRunDetailPage.jsx`

---

### Gap 6 — AC12: No pre-submission validation that checks attachment identifier against external service

**Layer:** Frontend

**AC:** AC12 — Error handling: flags mismatched attachment identifier before submission

**What is missing from the UI:** No pre-submission check validates whether the attachment identifier exists in the external invoicing service before the user clicks "Start Real Run". The user cannot confirm the identifier is valid until the run has already been attempted and failed. A "Verify Identifier" button or inline async lookup before run submission would close this gap.

**UI surface description:** Invoice Runs page → Batch PDF Attachment panel (`InvoiceRunPage.jsx`). The "Attachment Identifier" field has no "check" or "verify" affordance. The first user feedback on an invalid identifier is the run-level error banner that appears after the full run submission attempt.

**Files to change:**
- `/mnt/c/Users/drasm/Desktop/Invoicing/invoicing-fe/src/pages/runs/InvoiceRunPage.jsx`

---

## Verdict

GAPS_FOUND.done

## Fix Run
**Completed:** 2026-04-22T00:00:00Z
**Fixes applied:** 6
**Skipped:** 0
