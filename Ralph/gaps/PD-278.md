# Gap Report: PD-278

**Requirement:** 3.4.35 Error listing of events — mandatory field validation, configurable checks, error list creation, and prevention of invoicing for invalid events
**Scanned:** 2026-04-21T10:30:00Z
**Verdict:** PASSES

## Acceptance Criteria Coverage

| # | Criterion | Status | Notes |
|---|-----------|--------|-------|
| 1 | Event must have mandatory data (accounts, cost centers) before transfer to billing | IMPLEMENTED | `assertNoBlockingValidationFailures()` in `BillingEventTransferService` blocks transfer for FAILED events |
| 2 | System must support automated checks users can define (e.g. price matches price list) | IMPLEMENTED | `ValidationRule` entity with 4 types; full CRUD via `ValidationRuleController` + `ValidationRulesPage` |
| 3 | Alert for billing events exceeding quantity threshold (e.g. >30 emptyings) | IMPLEMENTED | `checkQuantityThreshold` reads field+threshold from rule config JSON |
| 4 | All billable events automatically validated before invoicing; error list generated | IMPLEMENTED | POST /validate endpoint; transfer and invoice generation both blocked for FAILED events |
| 5 | Validation covers customer info, pricing, VAT, accounting codes, service details | IMPLEMENTED | vatRate0, vatRate24, serviceResponsibility, billingProfile.businessId, billingProfile.billingAddress, billingProfile.eInvoiceAddress all added to mandatory field switch |
| 6 | Automatic data validation — mandatory fields present before processing | IMPLEMENTED | `BillingEventValidationService.validate()` runs all active rules against event fields |
| 7 | If required data missing, event is flagged and listed for review | IMPLEMENTED | Event marked FAILED; GET /{id}/validation-failures endpoint returns persisted logs; `ValidationFailuresList` rendered in `BillingEventDetailPage` |
| 8 | Configurable rules: price consistency checks against price list | IMPLEMENTED | `checkPriceConsistency` compares event prices against product defaults with tolerance% and verifies priceListId when configured |
| 9 | Thresholds for flagging unexpected invoice values | IMPLEMENTED | `QUANTITY_THRESHOLD` rule type with configurable field+threshold |
| 10 | Error list — clerks can review and correct before invoicing | IMPLEMENTED | `ValidationFailuresList` rendered in `ValidationStatusSection` on `BillingEventDetailPage` fetching persisted failures |
| 11 | Override certain validation checks with documented reason | IMPLEMENTED | POST /{id}/validation-override; reason + user + timestamp stored and displayed in `ValidationStatusSection` |
| 12 | Events cannot proceed to invoicing unless mandatory fields valid | IMPLEMENTED | Transfer blocked in `BillingEventTransferService`; invoice generation blocked in `InvoiceValidationEngine` |
| 13 | Audit and compliance — validation results logged with full traceability | IMPLEMENTED | `BillingEventValidationLog` persisted; GET /{id}/validation-failures endpoint exposes them per event |

## Files Checked

- `invoicing/src/main/java/com/example/invoicing/controller/billingevent/BillingEventController.java` (lines 307-310)
- `invoicing/src/main/java/com/example/invoicing/service/BillingEventValidationService.java` (lines 86, 94-95, 104-153, 155-232)
- `invoicing-fe/src/api/billingEvents.js` (line 174)
- `invoicing-fe/src/pages/billing/BillingEventDetailPage.jsx` (lines 19, 679)

## Verdict

PASSES
